//// Created by Kevin Tong on 4/10/2017.//#include <iostream>#include <utility>#include <algorithm>#include "payrollsystem.h"#include "paycheck.h"using namespace std;void PayrollSystem::addEmployee(string employeeID, string firstName, string lastName, double wage, int hours) {  Employee newEmployee(move(employeeID), move(firstName), move(lastName), wage, hours);  PayrollSystem::employees.push_back(newEmployee);}void PayrollSystem::findAndErase(string id) {  int counter = 0;  for (const auto &i : PayrollSystem::employees) {    if (id == i.getEmployeeId()) {      PayrollSystem::employees.erase(PayrollSystem::employees.begin() + counter);      break;    }    counter++;  }}void PayrollSystem::issuePaycheck(string id) {  Employee employee;  Paycheck paycheck;  int counter = 0;  for (const auto &i : PayrollSystem::employees) {    if (id == i.getEmployeeId()) {      double pay = employee.calcPay(i.getWage(), i.getHours());      paycheck.getPaycheck(i.getEmployeeId(), i.getFirstName(), i.getLastName(), pay);      break;    }    counter++;  }}